branches:
  only:
    - master
    - dev
    - /^release/*$/
    - /^hotfix/*$/

os: osx
osx_image: xcode11.2
language: node_js
node_js:
  - 12

script:
  - |
    npm run icon-gen &&
    npm run release -- -mwl -c.snap.publish=github
